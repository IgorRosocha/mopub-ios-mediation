    import XCTest
    @testable import mopub_ios_mediation

    final class mopub_ios_mediationTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(mopub_ios_mediation().text, "Hello, World!")
        }
    }
